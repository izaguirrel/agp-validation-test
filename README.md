# AGP Validation Test Repository

This is a test repository for verifying GUIDE-3140: AGP global validation commands in monorepos.

## Structure

- `module-a/` - First Maven module with commons-lang3 dependency
- `module-b/` - Second Maven module with jackson-databind dependency

## Purpose

Test that global validation commands configured in Guide UI apply to all projects in a monorepo.
